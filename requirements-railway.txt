# Railway constrained-memory runtime.
# Contextline enables RAG_LOW_MEMORY_MODE in the Docker image, using local BM25
# retrieval and the prebuilt language indexes without loading PyTorch or a
# sentence-transformer model. The Python standard library is sufficient.
